apiVersion: v1
kind: Service
metadata:
  name: vllm-router-direct
  labels:
    app: vllm-router
spec:
  type: NodePort
  selector:
    app: vllm-router
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 32401
      protocol: TCP
